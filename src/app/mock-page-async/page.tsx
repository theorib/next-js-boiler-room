export default async function MockPageAsync() {
  // const portfolio = await portfolioActions.getPortfolioItems();

  return (
    <div>
      <h1>I am the mock Page Async</h1>
      {/* <ul>
        {portfolio.map(item => (
          <li key={item.id}>{item.title}</li>
        ))}
      </ul> */}
    </div>
  );
}
